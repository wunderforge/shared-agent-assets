# Skills

Each immediate child directory is one installable Agent Skill and must contain
`SKILL.md`. Use `docs/templates/skill-template.md` to start a new skill.

Skills may include scripts, references, and assets, but all runtime capabilities
must be explicit. Every skill is scanned as a complete directory.

