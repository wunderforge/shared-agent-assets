# Memory

Store only durable, reusable, sanitized knowledge in `memory/<topic>/`.

Each memory document must state its provenance, scope, and `Last verified` date.
Do not store secrets, personal or customer data, raw transcripts, temporary task
state, or claims that cannot be reviewed.

